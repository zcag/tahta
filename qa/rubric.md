# Critique rubric

Score each dimension 1–5 (5 = excellent) and list concrete weaknesses. Be a harsh
reviewer: the job is to find faults a human would catch, not to confirm it works.

- **content** — Is it accurate, well-sequenced, and right for the audience and time
  budget in the brief? Any filler slides that say nothing?
- **variety** — Does the composition vary, or is it the same layout repeated? Flag
  monotony (e.g. "every slide is a centered title + bullets", "no components used,
  so no stats/callouts/diagrams enrich a single slide").
- **fit** — Does each slide's layout match its content shape (numbers→stat/metric,
  comparison→vs/compare, process→steps/timeline, definition→define)? Flag mismatches
  and slides that would be richer in a different layout or with a component.
- **polish** — Whitespace, balance, overflow, empty or near-empty slides, awkward
  short two-column slides, anything that reads unfinished.

Weaknesses must be specific and actionable ("slides 4–7 are all `statement` — break
the monotony with a `stats` or a `<Callout>`"), not vague ("could be better").
