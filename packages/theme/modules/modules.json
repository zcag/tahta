{
  "$comment": "Optional capability modules. Each is a prompt fragment a consumer (e.g. tela's MCP deck flow, or a CLI agent) appends to AGENTS.md ONLY when that capability is present in the run — so the always-served core contract stays lean. Append the file's contents after AGENTS.md when `when` holds.",
  "modules": [
    {
      "id": "branding",
      "file": "modules/branding.md",
      "when": "the deck has brand inputs to honor — a logo, brand color(s), or a brand name",
      "adds": "branding = accent (brand color, hue-matched not exact) + logo; the variant stays a free per-deck choice (no brand→variant mapping, don't converge on one); set lang"
    },
    {
      "id": "imagery",
      "file": "modules/imagery.md",
      "when": "the run can generate or place images (an image model is available, or the agent is adding background/concept/focal imagery)",
      "adds": "what imagery to make and where: imagery for 3 jobs only, reuse one background, the rich/on-palette/steps-10/raw quality recipe, isolate focal subjects on the variant ground, treatment (tahta-imagine) as a fallback, the generation contract, save-to-public determinism"
    }
  ]
}
